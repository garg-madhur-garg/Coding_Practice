class Solution{
public:

	vector<int> valueEqualToIndex(int arr[], int n) {
	    vector<int> v;
	    for(int i = 1; i <= n; i++){
	        if(arr[i - 1] == i){
	            v.push_back(i);
	        }
	    }
	    
	    return v;
	}
};
