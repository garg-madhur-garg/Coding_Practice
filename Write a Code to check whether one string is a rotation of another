#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s1;
    string s2;
    cin >> s1 >> s2;

    string temp = s1 + s1;
    if (temp.find(s2) != string::npos)
    {
        cout << "String are rotation of another" << endl;
    }
    else
    {
        cout << "Strings are not rotation of another" << endl;
    }
}
