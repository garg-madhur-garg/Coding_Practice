string reverseWord(string str){
    
  //Your code here
  int i = 0;
  int j = str.length() - 1;
  while(i <= j){
      int temp = str[j];
      str[j] = str[i];
      str[i] = temp;
      i++, j--;
  }
  
  return str;
}
